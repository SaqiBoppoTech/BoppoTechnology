import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainView: {
    backgroundColor: '#fff',
    flex: 1,
  },
  btn: {
    position: 'absolute',
    bottom: 10,
    left: 0,
    width: '100%',
  },
  titleWrapper: {
    marginTop: 10,
  },
});
