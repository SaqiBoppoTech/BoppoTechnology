import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    color: '#000',
    marginLeft: 15,
    fontSize: 18,
    fontWeight: '600',
    marginTop: 30,
    marginBottom: 10,
  },
  container: {
    marginTop: 450,
  },
});
